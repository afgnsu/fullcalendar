$('.calendar').fullCalendar('removeEvents', [<%= @calendar.id %>]);
$('.calendar').fullCalendar(
  'renderEvent',
  $.parseJSON("<%=j render(@calendar, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');
